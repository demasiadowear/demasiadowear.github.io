// Placeholder per ScrollTrigger plugin
console.log("ScrollTrigger placeholder - Questo file verrà sostituito con il plugin reale");

// Funzioni base per simulare il comportamento di ScrollTrigger
window.ScrollTrigger = {
  create: function(options) {
    console.log("ScrollTrigger creato con opzioni:", options);
    return {
      kill: function() { console.log("ScrollTrigger eliminato"); }
    };
  },
  refresh: function() {
    console.log("ScrollTrigger aggiornato");
  },
  update: function() {
    console.log("ScrollTrigger update");
  },
  defaults: {},
  batch: function() {},
  addEventListener: function() {},
  removeEventListener: function() {}
};

// Registra il plugin con GSAP se disponibile
if (window.gsap) {
  console.log("ScrollTrigger registrato con GSAP");
}
